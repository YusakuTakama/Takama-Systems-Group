# 2026-03-24 意思決定ログ

- **Inboxの整理**: 内容が「整理済み」となっているInboxファイルを削除し、ダッシュボードの「未整理件数」を正確に保つ運用とする。
- **MTG文字起こしデータの扱い**: 本日実施されるMTGのRAWデータ（約30分）は、ファイルとして保存（`.company/lab/meeting-logs/`など）した上で分析依頼を受けるワークフローとする。モデルは精度重視で **Pro** を推奨。
- **ダッシュボード形式の統一**: レンダラーによるアイコンのズレを防ぎつつ、ステータスを明確にするため以下の形式に統一する。
  - 進行中: `- [ ] (進行中) -> プロジェクト名`
  - 完了: `- [x] (完了) プロジェクト名`
- **同期方式の変更 (OneDrive一本化)**: Git による手動同期を廃止し、OneDrive の自動同期のみで運用する。Git 履歴に代わる「文脈の記録」として、秘書による `notes/` や `decisions.md` への詳細な自動記録をより一層強化する。
- **MTG要約ワークフローの確立**: RAWデータとユーザーメモを組み合わせた高精度なサマリー作成と、そこからの「次やること」のプロジェクトファイル（`pm/projects/`）への自動抽出・反映ルールを確定。
- **自動更新の制限と提案**: プロジェクトファイルの書き換えは独断で行わず、文脈から「完了」が推測される場合は「提案（確認）」を行う運用とする。
- **Notion連携の方針**: 短期・確実性は「Markdownエクスポート」、長期・利便性は「Notion MCP」の導入で解決する。明日のTODOに設定。
- **MCP環境の復旧**: 紛失した `gcp-oauth.keys.json` を全自動ではなく過去の履歴から手動で再構築し、`uv` を用いて正常に復旧完了。
