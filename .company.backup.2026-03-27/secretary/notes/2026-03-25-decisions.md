# 2026-03-25 意思決定ログ

## コミュニケーション・シンボルの導入
- **目的**: エージェント（秘書）による意図しない先行作業（「勝手に作業した」と感じさせる行為）を防止するため。
- **内容**: 
  - 文末の「？」は質問（作業停止）
  - 文末の「！」は判断を任せる（実行許可）
  - 句点（。）や記号なしは、指示として受け取るが迷う場合は確認する

## MTG議事録およびダッシュボードのSkill化
- **内容**: 
  - 現在 `RULES.md` に記載されている複雑なマニュアルを `.agents/skills/` フォルダに移管し、機能をモジュール化する。
  - 明日（2026-03-26）からプロトタイプの実装を開始する。

## Workout Tracker UX v2.1 改善
- **内容**: 
  - ジムでの実用性を最優先し、手動の「Save Workout」ボタンを廃止、全ての入力を `onChange` で即時オートセーブする仕様に変更。
  - Reps（回数）入力をカスタムSwipeコンポーネント案から、OS標準のドラムロール（`<select>`） pickerへ変更し、確実性と速度を向上。

## Workout Tracker UX v2.4 改善（最終形態）
- **内容**: 
  - 親要素の `overflow-hidden` による見切れを React Portal (`createPortal`) で根本解決。
  - `getBoundingClientRect` と `-translate-y-1/2` により、元の数字とミリ単位で重なるオーバーレイを実現。
  - `useLayoutEffect` と `behavior: 'instant'` による「チラつきゼロ」の初期表示。
  - 上下 Padding による 0/100 の中央配置、および全黒テキスト＋テーマハイライトによる視認性向上。

## Workout Tracker UX v2.5 改善（ジャンプ解消）
- **内容**: 
  - `scrollIntoView` がドキュメント全体のスクロール位置を狂わせる問題（約286pxのジャンプ）を特定。
  - 計算に基づいた `.scrollTop` の手動設定に切り替え、ページ全体のレイアウトシフトをゼロに抑えたまま中央表示を実現。
  - Tailwind v4 のコンパイル停滞を回避するため、`py-[]` 内の `calc` を固定値 `py-[84px]` に最適化。
